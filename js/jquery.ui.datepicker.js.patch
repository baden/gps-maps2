*** D:\Work\MAIN\GPS\GGT-200\SRC\SITE\googleapp\gps-maps2\plugins\jquery-ui-1.8.7\jquery-ui-1.8.7\ui\jquery.ui.datepicker.js	Wed Dec  8 11:44:48 2010
--- D:\Work\MAIN\GPS\GGT-200\SRC\SITE\googleapp\gps-maps2\js\jquery.ui.datepicker.js	Mon Jan 10 22:15:04 2011
***************
*** 902,913 ****
  		var onSelect = this._get(inst, 'onSelect');
  		if (onSelect)
  			onSelect.apply((inst.input ? inst.input[0] : null), [dateStr, inst]);  // trigger custom callback
  		else if (inst.input)
  			inst.input.trigger('change'); // fire the change event
! 		if (inst.inline)
! 			this._updateDatepicker(inst);
  		else {
  			this._hideDatepicker();
  			this._lastInput = inst.input[0];
  			if (typeof(inst.input[0]) != 'object')
  				inst.input.focus(); // restore focus
--- 902,913 ----
  		var onSelect = this._get(inst, 'onSelect');
  		if (onSelect)
  			onSelect.apply((inst.input ? inst.input[0] : null), [dateStr, inst]);  // trigger custom callback
  		else if (inst.input)
  			inst.input.trigger('change'); // fire the change event
! 		//if (inst.inline)
! 		//	this._updateDatepicker(inst);
  		else {
  			this._hideDatepicker();
  			this._lastInput = inst.input[0];
  			if (typeof(inst.input[0]) != 'object')
  				inst.input.focus(); // restore focus
